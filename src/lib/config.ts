// ─── Datos de la distribuidora que ve el cliente ──────────────────────────────
// Lo que esté en null no se muestra en la app. Completar con los datos reales;
// más adelante pueden venir del ERP (por cliente o por zona de reparto).

// Número de WhatsApp que recibe los pedidos (formato internacional, sin + ni espacios)
export const WHATSAPP_PEDIDOS = '541159320255';

// Ej: '18:00' → "Pedí hasta las 18:00"
export const HORA_CORTE_PEDIDOS: string | null = null;

// Ej: 'Lunes a viernes'
export const DIAS_REPARTO: string | null = null;
